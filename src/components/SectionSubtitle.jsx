import React from 'react';

const SectionSubtitle = ({ children }) => (
  <h3 className="subtitle is-size-4-tablet is-size-5-mobile has-text-centered-mobile">
    {children}
  </h3>
);

export default SectionSubtitle;
